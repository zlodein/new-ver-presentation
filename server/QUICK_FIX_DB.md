# Быстрое исправление подключения к базе данных

## Шаг 1: Найти путь к проекту на сервере

Выполните одну из команд для поиска проекта:

```bash
# Вариант 1: Поиск по стандартному пути FastPanel
ls -la /var/www/*/data/www/*/server/package.json 2>/dev/null | head -1

# Вариант 2: Поиск по имени базы данных в .env
find /var/www -name ".env" -type f -exec grep -l "e_presentati" {} \; 2>/dev/null | head -1

# Вариант 3: Поиск по файлу check-db-connection.js
find /var/www -name "check-db-connection.js" -type f 2>/dev/null | head -1

# Вариант 4: Если знаете домен, проверьте стандартный путь
ls -la /var/www/e_presentati_usr/data/www/e-presentation.ru/server/package.json
```

## Шаг 2: Перейти в директорию server

После того как нашли путь, перейдите в директорию:

```bash
# Замените путь на найденный выше
cd /var/www/e_presentati_usr/data/www/e-presentation.ru/server

# Или если другой путь:
cd /путь/к/проекту/server
```

## Шаг 3: Проверить и исправить .env

```bash
# Проверить текущий DATABASE_URL
cat .env | grep DATABASE_URL

# Отредактировать .env (замените YOUR_PASSWORD_HERE на реальный пароль)
nano .env
```

Убедитесь, что строка имеет формат:
```
DATABASE_URL=postgresql://e_presentati:ВАШ_ПАРОЛЬ@localhost:5432/e_presentati
```

**ВАЖНО:** Если пароль содержит специальные символы (`&`, `>`, `<`, `@`, `;` и т.д.), их нужно закодировать в URL или использовать альтернативный формат (см. ниже).

## Шаг 4: Проверить подключение к базе данных

```bash
# Установить зависимости, если еще не установлены
npm install

# Проверить подключение
npm run db:check
```

## Шаг 5: Создать таблицы (если их нет)

Если скрипт проверки показал, что таблицы отсутствуют:

```bash
npm run db:push
```

## Шаг 6: Перезапустить backend

```bash
# Если используете systemd
systemctl restart presentation-backend
systemctl status presentation-backend

# Проверить логи
journalctl -u presentation-backend -n 50
```

## Альтернативный формат DATABASE_URL (если пароль со спецсимволами)

Если пароль содержит специальные символы, используйте этот формат в `.env`:

```env
DB_USER=e_presentati
DB_PASSWORD=ваш_пароль_со_спецсимволами
DB_NAME=e_presentati
DB_HOST=localhost
DB_PORT=5432
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
```

**Примечание:** Node.js не поддерживает переменные окружения в таком формате напрямую. Нужно либо:
1. Закодировать спецсимволы в URL (см. таблицу ниже)
2. Или использовать другой способ (например, скрипт для генерации DATABASE_URL)

### Кодирование спецсимволов в URL:

- `@` → `%40`
- `&` → `%26`
- `>` → `%3E`
- `<` → `%3C`
- `;` → `%3B`
- `:` → `%3A` (но не в части user:password)
- `/` → `%2F`
- `?` → `%3F`
- `#` → `%23`
- `[` → `%5B`
- `]` → `%5D`
- ` ` (пробел) → `%20`

Пример:
```
Пароль: g&7W>0@z;fGznoDz
Закодированный: g%267W%3E0%40z%3BfGznoDz
DATABASE_URL=postgresql://e_presentati:g%267W%3E0%40z%3BfGznoDz@localhost:5432/e_presentati
```

## Быстрая проверка подключения через psql

Если хотите проверить подключение напрямую через psql:

```bash
# Подключиться к базе данных
sudo -u postgres psql -d e_presentati

# Или если пользователь e_presentati существует:
psql -U e_presentati -d e_presentati

# Проверить наличие таблиц
\dt

# Выйти
\q
```

## Проверка работы после исправления

1. Проверьте логи backend на наличие ошибок
2. Попробуйте зарегистрироваться через форму
3. Попробуйте войти через форму авторизации
4. Попробуйте восстановить пароль

Если все работает — проблема решена! 🎉
