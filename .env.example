# URL бэкенда API (авторизация, презентации, подсказки адресов, GigaChat, метро). Без слэша в конце.
# Запустите сервер (server): npm run dev в папке server, порт задаётся в server/.env (PORT=3002).
# Пример: http://localhost:3002
VITE_API_URL=
