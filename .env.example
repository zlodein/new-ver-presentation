# URL бэкенда API (авторизация, презентации и т.д.). Без слэша в конце.
# Локальная разработка: задайте URL сервера, например http://localhost:3002 (порт в server/.env).
# Прод-сборка: можно оставить пустым — запросы пойдут на тот же домен (/api).
VITE_API_URL=

# Dadata.ru — API-ключ для подсказок адресов (виджет vue-dadata). Только для подсказок, ключ доступен в браузере.
VITE_DADATA_API_KEY=
